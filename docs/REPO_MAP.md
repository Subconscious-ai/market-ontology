# Repository Map — market-ontology

> **Generated by `scripts/generate_repo_map.py` — do not edit by hand.**
> Regenerate after any schema or structure change. CI runs
> `generate_repo_map.py --check` and fails if this file is stale.
> The map is a deterministic projection of the repo (no timestamp, no
> commit hash), so `--check` is reproducible.

`market-ontology` is the canonical Pydantic schema for the Subconscious
knowledge graph. `poc_v1/ontology/schema.py` is the single source of truth
for graph shape; everything below is derived from it.

## Schema

- **SCHEMA_VERSION:** `1.6.0`
- **Node models (14):** `Attribute`, `AttributeLevel`, `Company`, `Estimate`, `Evidence`, `ExperimentRun`, `Market`, `Need`, `Offering`, `Stage`, `StakeholderArchetype`, `Trait`, `TraitLevel`, `Transition`
- **Edge models (18):** `ABOUT`, `ADDRESSES`, `COMPETES_WITH`, `CONSUMED`, `FROM`, `HAS_ATTRIBUTE`, `HAS_LEVEL`, `HAS_NEED`, `HAS_TRAIT`, `IN_MARKET`, `OFFERED_BY`, `OFFERING_IN_MARKET`, `PRODUCED`, `RELEVANT_AT`, `RELEVANT_TO`, `SUPPORTS`, `TARGETS_STAKEHOLDER`, `TO`

## Public import surface

Every downstream consumer (spice-harvester, ai-chatbot-native-sizzle,
twenty CRM, burn-substrate) imports these modules directly:

- `poc_v1.ontology.schema` — NODE_MODELS, EDGE_MODELS, SCHEMA_VERSION — the single source of truth.
- `poc_v1.ontology.graphiti_views` — ENTITY_TYPES, EDGE_TYPES, EDGE_TYPE_MAP — derived graphiti-compatible view.
- `poc_v1.ontology.identity` — CompanyIdentity, to_identity, normalize_slug — email/domain/URL → slug.
- `poc_v1.ontology.iri` — to_iri, class_iri, predicate_iri, property_iri — canonical RDF IRIs.

## Top-level layout

- `causal_dag_v1/` — Causal peer module — causal hypotheses (CAUSES/MEDIATES), NetworkX acyclicity.
- `docs/` — Architecture diagrams, ADRs, and this generated map.
- `poc_v1/` — Ontology package — schema, public modules, contracts, kg_seed fixtures.
- `scripts/` — Generators, validators, and the scripts/agent/ harness.
- `tests/` — Unit and contract tests (python -m unittest discover -s tests).

## Generated artifacts

Under `poc_v1/ontology/` — regenerated from the schema, never hand-edited:

- `edge_schemas.json`
- `kg_seed_contract.json`
- `node_schemas.json`
- `super_ego_projection.json`
- `trustgraph_ontology.json`
- `trustgraph_projection.json`
- `twenty_app_contract.json`
- `twenty_projection.json`
- `trustgraph_projection.ttl`

## Entry points

- Install: `pip install -e ".[dev]"`
- Fast gate (run before any PR): `bash scripts/agent/validate-fast.sh`
- Regenerate this map: `python scripts/generate_repo_map.py`
- Agent guidance: `CLAUDE.md` (canonical; `AGENTS.md` / `GEMINI.md` symlink to it).

